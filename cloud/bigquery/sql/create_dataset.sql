-- Replace PROJECT_ID and DATASET_ID before running.
CREATE SCHEMA IF NOT EXISTS `PROJECT_ID.DATASET_ID`
OPTIONS (
  location = 'US',
  description = 'PlayStation Game Success analytics dataset for the cloud bonus workflow'
);

